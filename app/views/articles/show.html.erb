<div class="container mt-5">
  <h1><%= @article.title %></h1>
  <h3><%= @article.content %></h3>

  <%= link_to "Edit #{@article.title}", edit_article_path(@article) %>
  <br>
  <%= link_to "Delete Article", article_path(@article), data: { turbo_method: :delete, turbo_confirm: "Are your sure?" } %>
</div>
